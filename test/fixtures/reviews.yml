# test/fixtures/reviews.yml
one:
  rating: 5
  comment: "Great quality!"
  order: one        # same for order, customer, and store
  customer: one
  store: one
  product: one      # ← here!
  created_at: <%= Time.current %>
  updated_at: <%= Time.current %>

two:
  rating: 4
  comment: "Good value for money."
  order: two
  customer: two
  store: two
  product: one      # could use another product fixture, if it exists
  created_at: <%= Time.current %>
  updated_at: <%= Time.current %>