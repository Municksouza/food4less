<h1><%= @product.name %></h1>
<p><%= @product.description %></p>
<p>Preço: <%= number_to_currency(@product.price) %></p>
<p>Preço Anterior: <%= number_to_currency(@product.old_price) %></p>
<p>Estoque: <%= @product.stock %></p>

<div class="product-images">
  <% @product.images.each do |image| %>
    <%= cl_image_tag image.key, width: 300, height: 300, crop: :fill %>
  <% end %>
</div>

<%= link_to "Back", stores_products_path(@store.id) %>