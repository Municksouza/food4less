<h2>Products</h2>
<% if @product_results.any? %>
  <ul>
    <% @product_results.each do |product| %>
      <li><%= link_to product.name, stores_product_path(product) %></li>
    <% end %>
  </ul>
<% else %>
  <p>No products found.</p>
<% end %>