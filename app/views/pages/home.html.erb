<div class="container mt-4">
  <h1 class="mb-4">Featured Products</h1>

  <div class="row">
    <% @products.each do |product| %>
      <div class="col-md-4">
        <%= render partial: "stores/products/product_card", locals: { product: product } %>
      </div>
    <% end %>
  </div>
</div>